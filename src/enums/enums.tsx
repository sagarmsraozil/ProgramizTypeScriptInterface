export enum LEARNING_PATH_SOURCE_TYPES {
  RECOMMENDED,
  COURSE_IN_PROGRESS,
  LEARNING_PATH_IN_PROGRESS,
  LEARNING_PATH_ALMOST_COMPLETED,
  NO_LEARNING_PATH_AND_COURSE_PROGRESS,
  NO_LEARNING_PATH_BUT_COURSE_PROGRESS,
}

export enum COURSE_ENROLLMENT_STATUS {
  NEW_COURSE_RECOMMENDATION,
  ENROLLED_IN_A_COURSE_DIRECTLY,
}

export enum COURSE_TAB_INDICES {
  IN_PROGRESS,
  COMPLETED,
}

export enum TAB_INDICES {
  LEARNING_PATH,
  COURSES,
  CHALLENGES,
}

export enum FEEDBACK_STEPS {
  EMOJI,
  FEEDBACK_FORM,
  THANK_YOU,
}

export enum COURSE_POPUP_STATUSES {
  THROUGH_LEARNING_PATH,
  THROUGH_COURSE,
}

export enum COURSE_PROGRESS_STATUSES {
  COMPLETED,
  IN_PROGRESS,
}

export enum CODE_TYPES {
  EXAMPLE,
  COMPILER,
}

export enum SEARCH_SOURCES {
  NAVBAR,
  PAGE,
}

export enum SEARCH_TABS {
  LEARNING_PATH,
  COURSE,
}

export enum STATUS_MESSAGE_TYPES {
  SUCCESS,
  ERROR,
}

export enum EDITOR_TYPES {
  EDITOR,
  SHELL,
  TERMINAL,
}

export enum CHALLENGE_STATUSES {
  NOT_TESTED,
  CORRECT,
  WRONG,
  RETRY_CLICKED,
}

export enum QUIZ_STATUSES {
  NO_ANSWER,
  CORRECT,
  WRONG,
}

export enum LESSON_TYPES {
  LESSON_EXAMPLE,
  QUIZ,
  CHALLENGE,
  VIDEO,
}

export enum PAYMENT_TYPES {
  PADDLE,
  PLAID,
}

export enum SECTION_CONTENT_TYPE {
  LESSON,
  QUIZ,
  CHALLENGE,
  EXAMPLE,
  VIDEO,
}

export enum SECTION_CONTENT_TYPE_ID {
  LESSON,
  QUIZ,
  CHALLENGE,
  EXAMPLE,
  VIDEO,
}

export enum CERTIFICATE_TYPES {
  COURSE,
  LEVEL,
  LEARNING_PATH,
}
